<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@drawable/splash_bck">

        <TextView
            android:id="@+id/lbl_expire"
            android:layout_width="0dp"
            android:layout_height="0dp"
            android:layout_margin="@dimen/dimen_15_dp"
            android:background="@drawable/round_edge_button_blue_bck"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            android:textColor="@color/white"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintHeight_percent="0.07"
            android:text="@string/lbl_expiring"
            android:gravity="center"
            android:textSize="@dimen/text_size_25_sp"
            />

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/recy_expiryList"
            android:layout_width="0dp"
            android:layout_height="0dp"
            app:layout_constraintTop_toBottomOf="@id/lbl_expire"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="@id/lbl_expire"
            app:layout_constraintEnd_toEndOf="@id/lbl_expire"
            app:layoutManager="androidx.recyclerview.widget.LinearLayoutManager"
            tools:listitem="@layout/child_list"
            tools:itemCount="10"
            android:layout_margin="@dimen/dimen_10_dp"
            />
    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>